plugins {
    id 'java'
    id "com.github.johnrengelman.shadow" version "6.1.0"
}

shadowJar {
    project.configurations.implementation.canBeResolved(true)
    configurations = [project.configurations.implementation]
}

group = 'fr.kx2zh.minaria.hub'
version = '1.0-SNAPSHOT'

sourceCompatibility = 1.8
targetCompatibility = 1.8

compileJava.options.encoding = 'UTF-8'

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    compileOnly 'org.spigotmc:spigot:1.8.8-R0.1-SNAPSHOT'
    implementation group: 'fr.kx2zh.minaria', name: 'minaria-api', version: '0.1'
    implementation group: 'fr.kx2zh.shadow', name: 'shadow-api', version: '0.1'
    implementation group: 'org.redisson', name: 'redisson', version: '3.2.0'
}

processResources {
    def props = [version: version]
    inputs.properties props
    filteringCharset 'UTF-8'
    filesMatching('plugin.yml') {
        expand props
    }
}
